<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>V-Void shortener</title>
  <link rel="icon" type="image/x-icon" href="/images/icon.svg">
</head>
<style nonce={{styleN}}>
  
  :root {
    --base-color: #2B303B;
    --button-color: #a1a1a1;
    --button-color-2: white;
    --hover-color: #494e59;
  }

  @font-face {
    font-family: 'Silkscreen';
    font-style: normal;
    font-weight: 400;
    src: url(/fonts/Silkscreen-Regular.ttf) format('trueType');
  }

  @font-face {
    font-family: 'Silkscreen';
    font-style: normal;
    font-weight: 700;
    src: url(/fonts/Silkscreen-Bold.ttf) format('trueType');
  }

  @font-face {
    font-family: 'Ubuntu';
    font-style: normal;
    font-weight: 200;
    src: url(/fonts/Ubuntu-Light.ttf) format('trueType');
  }

  @font-face {
    font-family: 'Ubuntu';
    font-style: normal;
    font-weight: 300;
    src: url(/fonts/Ubuntu-Regular.ttf) format('trueType');
  }

  @font-face {
    font-family: 'Ubuntu';
    font-style: normal;
    font-weight: 400;
    src: url(/fonts/Ubuntu-Medium.ttf) format('trueType');
  }

  @font-face {
    font-family: 'Ubuntu';
    font-style: normal;
    font-weight: 700;
    src: url(/fonts/Ubuntu-Bold.ttf) format('trueType');
  }

  body {
    padding: 0;
    margin: 100px auto;
    min-height: 100%;
    width: 100%;
    background-color: var(--base-color);
    /* background-color: #08AEEA; */
    /* background-image: linear-gradient(0deg, #08AEEA 0%, #2AF598 100%); */
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .shortener__url-container {
    box-sizing: border-box;
    background-color: white;
    min-height: 700px;
    width: 1400px;
    box-shadow: -15px 20px 0px 3px rgba(0,0,0,1);
    padding: 50px 50px 50px 50px;
    display: flex;
    flex-direction: column;
    justify-content: space-around;
  }

  .shortener__url-title_users,
  .shortener__url-title-welcome,
  .shortener__url-title {
    font-family: 'Silkscreen', monospace;
    font-weight: 700;
    font-size: 80px;
  }

  .shortener__url-title-welcome {
    text-align: center;
  }

  .shortener__url-title_users {
    text-align: center;
  }

  .store__file-url,
  .store__select-file,
  .shortener__url-input {
    font-family: 'Ubuntu', sans-serif;
    box-sizing: border-box;
    margin-top: 50px;
    height: 55px;
    width: 100%;
    border-radius: 0px;
    border: 2px solid black;
    font-weight: 300;
    font-size: 25px;
    padding: 10px;
  }

  .store__select-file {
    padding: 0;
  }

  .store__file-url {
    height: 100px;
  }

  input::file-selector-button {
    font-family: 'Silkscreen', monospace;
    cursor: pointer;
    font-weight: bold;
    height: 55px;
    width: 300px;
    border: none;
    margin: 0;
    margin-right: 20px;
    border-radius: none;
    transition: all .1s;
    background-color: var(--button-color);
    padding-bottom: 7px;
  }

  input[type="file"]::file-selector-button:hover {
    background-color: var(--hover-color);
    color: white;
  }

  .shortener__404 {
    text-align: center;
  }

  .shortener__404-title {
    font-family: 'Silkscreen', monospace;
    font-weight: 700;
    font-size: 100px;
  }

  .shortener__url-subtitle {
    font-family: 'Silkscreen', monospace;
    font-weight: 400;
    font-size: 40px;
  }

  .shortener__url-button {
    box-sizing: border-box;
    margin: 0px;
    margin-top: 30px;
    height: 50px;
    width: 100%;
    font-family: 'Silkscreen', monospace;
    border-radius: 0;
    background-color: var(--button-color);
    color: black;
    transition: all .1s;
    border: 2px solid black;
    font-size: 25px;
  }

  .shortener__url-button:disabled:hover {
    cursor: default;
    transform: none;
    box-shadow: none;
    background-color: var(--button-color);
    color: black;
  }

  .shortener__error {
    font-family: 'Silkscreen', monospace;
    color: red;
    width: 100%;
    height: 20px;
    font-size: 25px;
  }

  .shortener__user-url__container {
    display: flex;
    width: 100%;
    margin-bottom: 20px;
  }

    .shortener__user-url__container div:first-of-type {
    width: 70%; 
    margin-right: 20px
  }

  .shortener__user-url__container div:last-of-type {
    width: 30%;
  }

  .shortener__url-button__list {
    box-sizing: border-box;
    margin: 0 0 15px 0;
    height: 55px;
    width: 100%;
    font-family: 'Silkscreen', monospace;
    border-radius: 0;
    background-color: var(--button-color);
    color: black;
    transition: all .1s;
    border: 2px solid black;
    font-size: 25px;
  }

    .shortener__url-input__list {
    font-family: 'Ubuntu', sans-serif;
    box-sizing: border-box;
    margin: 0 0 15px 0;
    height: 55px;
    width: 100%;
    border-radius: 0px;
    border: 2px solid black;
    font-weight: 300;
    font-size: 25px;
    padding: 10px;
    transition: all .1s;
    word-break: break-all;
    overflow: hidden;
  }

  #urls-list {
    padding: 0;
  }

  .shortener__url-button:hover,
  .shortener__url-button__list:hover {
    cursor: pointer;
    transform: translateY(-5px);
    box-shadow: -5px 10px 0px 3px rgba(0,0,0,1);
    background-color: var(--hover-color);
    color: white;
  }

</style>
<body>
  <div class="shortener__url-container">
  {{{body}}}
  </div>
</body>
</html>